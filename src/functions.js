/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable @typescript-eslint/no-empty-function */
/**
 * 
 * @param {Array<Template>} newTemplates 
 * @returns {Card}
 */
function initializationCard(newTemplates) {}

/**
 * 
 * @param {string} templateName 
 * @param {Card} card 
 * @returns {Card}
 */
function applyTemplate(templateName, card) {}

/**
 * 
 * @param {Canvas} newCanvas 
 * @param {string} templateName 
 * @param {Card} card 
 * @returns {Card}
 */
function addTemplate(newCanvas, templateName, card) {}

/**
 * 
 * @param {string} templateName 
 * @param {Card} card 
 * @returns {Card}
 */
function deleteTemplate(templateName, card) {}

/**
 * 
 * @param {Card} card 
 * @returns {Card}
 */
function madeChange(card) {}

/**
 * 
 * @param {Card} card 
 * @returns {Card}
 */
function redoRollback(card) {}

/**
 * 
 * @param {Card} card 
 * @returns {Card}
 */
function undoRollback(card) {}

/**
 * 
 * @param {Size} newSize 
 * @param {Card} card 
 * @returns {Card}
 */
function setSizeCanvas(newSize, card) {}

/**
 * 
 * @returns {Card}
 */
function deleteCanvas() {}

/**
 * 
 * @param {string} color 
 * @param {Card} card 
 * @returns {Card}
 */
function setBackgroundColor(color, card) {}

/**
 * 
 * @param {string} url 
 * @param {Card} card 
 * @returns {Card}
 */
function setBackgroundImage(url, card) {}

/**
 * 
 * @param {Card} card 
 * @returns {Card}
 */
function selectElement(card) {}

/**
 * 
 * @param {Point} mousePoint 
 * @param {Card} card 
 * @returns {Card}
 */
function moveSelectElement(mousePoint, card) {}

/**
 * 
 * @param {Element} oldElement 
 * @param {Card} card
 * @returns {Card} 
 */
function deleteSelectElement(oldElement, card) {}

/**
 * 
 * @param {number} elementId 
 * @param {Card} card 
 * @returns {Card}
 */
function updateSelectElement(elementId, card){}

/**
 * 
 * @param {Card} card 
 * @returns {Card}
 */
function addText(card) {}

/**
 * 
 * @param {Size} newSize 
 * @param {Card} card 
 * @returns {Card}
 */
function resizeText(newSize, card) {}

/**
 * 
 * @param {number} newSize 
 * @param {Card} card 
 * @returns {Card}
 */
function setSizeText(newSize, card) {}

/**
 * 
 * @param {string} newFont 
 * @param {Card} card 
 * @returns {Card}
 */
function setFontText(newFont, card) {}

/**
 * 
 * @param {string} newColor 
 * @param {Card} card 
 * @returns {Card}
 */
function setColorText(newColor, card) {}

/**
 * 
 * @param {string} newText 
 * @param {Card} card 
 * @returns {Card}
 */
function changeText(newText, card) {}

/**
 * 
 * @param {Card} card 
 * @returns {Card}
 */
function addImage(card) {}

/**
 * 
 * @param {Size} newSize 
 * @param {Card} card 
 * @returns {Card}
 */
function resizeImage(newSize, card) {}

/**
 * 
 * @param {string} newColor 
 * @param {Card} card 
 * @returns {Card}
 */
function setColorFigure(newColor, card) {}

/**
 * 
 * @param {Card} card 
 * @returns {Card}
 */
function addTriangle(card) {}

/**
 * 
 * @param {Point} mousePoint 
 * @param {Card} card 
 * @returns {Card}
 */
function changeFirstPoint(mousePoint, card) {}

/**
 * 
 * @param {Point} mousePoint 
 * @param {Card} card 
 * @returns {Card}
 */
function changeSecondPoint(mousePoint, card) {}

/**
 * 
 * @param {Point} mousePoint 
 * @param {Card} card 
 * @returns {Card}
 */
function changeThirdPoint(mousePoint, card) {}

/**
 * 
 * @param {Card} card 
 * @returns {Card}
 */
function addRectangle(card) {}

/**
 * 
 * @param {Point} mousePoint 
 * @param {Card} card 
 * @returns {Card}
 */
function resizeRectangle(mousePoint, card) {}

/**
 * 
 * @param {Point} mousePoint 
 * @param {Card} card 
 * @returns {Card}
 */
function setWidthRectangle(mousePoint, card) {}

/**
 * 
 * @param {Point} mousePoint 
 * @param {Card} card 
 * @returns {Card}
 */
function setHeightRectangle(mousePoint, card) {}

/**
 * 
 * @param {Card} card 
 * @returns {Card}
 */
function addCircle(card)  {}

/**
 * 
 * @param {PositionOptions} mousePoint 
 * @param {Card} card 
 * @returns {Card}
 */
function setRadiusCircle(mousePoint, card){}